// Dependencies
const path = require("path")
const express = require("express");
const { ApolloServer } = require("apollo-server-express");
// Imported variables (schemas, auth middleware, db connection)

// Server configuration
// Initialize port connectioon for production & local machine
// Initialize new Apollo Server instance
// Initialize Express server & middleware
// IF deployed to production, use client side build for frontend code 
// Initialize & call Apollo Server with GraphQL schema parameters.